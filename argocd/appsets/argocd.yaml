---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argocd
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - name: in-cluster
  template:
    metadata:
      name: "argocd"
    spec:
      project: my-project
      sources:
        - repoURL: https://github.com/voidlily/kubelab
          targetRevision: HEAD
          path: argocd/apps/argocd/base/
      destination:
        name: in-cluster
      syncPolicy:
        automated:
          enabled: true
          prune: true
          selfHeal: true
